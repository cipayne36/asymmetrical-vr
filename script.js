function generateMaze() {
	//how do we generate a maze?
}

function serverConnect() {
	//how do we connect to a server
}

function detectSurface() {
	//do we want to detect a surface for the maze, after class suggestion?
}